#include <tlpi_hdr.h>
#include "header/kv.h"

int main()
{
	/* test for kv library */
	// kv_dsa *kv, *p;

	// if ((kv = init_kv()) == NULL)
	// 	errExit("init_kv");
	// if (add_kv(kv, "apple", "pingguo") == -1)
	// 	errExit("add_kv");
	// if (add_kv(kv, "banana", "xiangjiao") == -1)
	// 	errExit("add_kv");
	// if (add_kv(kv, "canada", "jianada") == -1)
	// 	errExit("add_kv");
	// if (delete_kv(kv, "canada") == -1)
	// 	errExit("delete");
	// if (delete_kv(kv, "apple") == -1)
	// 	errExit("delete");
	// if (add_kv(kv, "japan", "riben") == -1)
	// 	errExit("add_kv");
	// if (add_kv(kv, "rust", "pangxie") == -1)
	// 	errExit("add_kv");
	// if (delete_kv(kv, "rust") == -1)
	// 	errExit("delete");
	// if (change_kv(kv, "banana", "a") == -1)
	// 	errExit("change_kv");
	// if (change_kv(kv, "japan", "b") == -1)
	// 	errExit("change_kv");

	// for (p = kv->next; p; p = p->next)
	// 	printf("(%s : %s)\n", p->key, p->value);
	/* end test for kv library*/

	return 0;
}