// #include <stdio.h>
// #include <string.h>
// #include <sys/resource.h>
// #include <errno.h>
#include <tlpi_hdr.h>

int main(int argc, char *argv[])
{
	pid_t pid;

	// errno = 0;
	// if ((nice = getpriority(PRIO_PROCESS, pid)) == -1 && errno != 0)
	// 	errExit("getpriority");

	return 0;
}
